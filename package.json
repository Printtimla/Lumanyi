{
  "name": "lumanyi",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "test": "vitest",
    "cf-typegen": "wrangler types",
    "typecheck": "tsc --noEmit",
    "ci": "npm run typecheck && npm test",
    "db:migrate:local": "wrangler d1 migrations apply lumanyi --local"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.12.4",
    "@types/node": "^26.1.1",
    "typescript": "^5.5.2",
    "vitest": "~3.2.0",
    "wrangler": "^4.114.0"
  },
  "dependencies": {
    "hono": "^4.12.32"
  }
}
